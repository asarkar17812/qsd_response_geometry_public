# Python dependencies for the verification scripts, figure scripts, and walkthrough notebook.
# Tested on CPython 3.13 with the versions below; any recent NumPy/Matplotlib should work.
numpy>=1.24
matplotlib>=3.6

# For the notebook only:
# jupyter (or: pip install nbconvert ipykernel  to run it headless)
